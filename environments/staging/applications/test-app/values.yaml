image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

##
## Application
##

controller:
  type: deployment
  replicas: 1
  strategy: RollingUpdate

service:
  main:
    enabled: true
    primary: true
    type: ClusterIP
    ports:
      http:
        port: 80

secrets:
  secretenv:
    enabled: false
    stringData: {}
      # TEST: "<path:/secrets/clusters/sourcify/shared/shared.enc.yaml#test>"
